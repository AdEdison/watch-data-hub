name: Update Watch Data

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"

permissions:
  contents: write

jobs:
  update-watch-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show current data files
        run: |
          ls -lah
          ls -lah data

      - name: Placeholder
        run: |
          echo "Later we will run Python scripts here to update CSV files."

      - name: Commit changes if any
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git status
